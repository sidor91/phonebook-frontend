import styled from 'styled-components';

const FilterLabel = styled.label`
  max-width: 400px;
`;
const FilterLabelName = styled.span`
font-size: 16px;
margin-right: 10px;
`;
const FilterInput = styled.input`
`;

export { FilterLabel, FilterInput, FilterLabelName };